# Pastebin Lite

A minimal Pastebin-like application built with Next.js.

## Features
- Create text pastes
- Optional TTL and view limits
- Shareable URLs
- API + HTML views
- Deterministic time testing

## Local Setup
npm install
npm run dev

## Persistence
Upstash Redis is used for persistence across serverless requests.
